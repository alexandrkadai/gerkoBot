services:
  - type: web
    name: gerkobot
    runtime: node
    buildCommand: npm install && npm run build && echo "=== Build complete, checking dist ===" && ls -laR dist
    startCommand: node dist/server.js
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
